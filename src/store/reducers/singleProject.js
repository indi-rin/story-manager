import axios from "axios";

// action types

// action creators

// thunks

// reducer
const singleProjectReducer = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default singleProjectReducer;
