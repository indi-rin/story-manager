import axios from "axios";

// action types

// action creators

// thunks

// reducer
const allProjectsReducer = (state = [], action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default allProjectsReducer;
